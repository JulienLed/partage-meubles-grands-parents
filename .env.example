# Neon PostgreSQL — pooled connection string (utilisé par Prisma en production)
DATABASE_URL=

# Neon PostgreSQL — connexion directe (pour prisma migrate, sans pooler)
DATABASE_URL_UNPOOLED=

# Vercel Blob — disponible après linkage du projet sur Vercel
BLOB_READ_WRITE_TOKEN=
